<?php


/**
 * Base class that represents a query for the 'configuracion' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Wed Apr 11 12:20:04 2018
 *
 * @method ConfiguracionQuery orderById($order = Criteria::ASC) Order by the id column
 * @method ConfiguracionQuery orderByHost($order = Criteria::ASC) Order by the host column
 * @method ConfiguracionQuery orderByEncriptacion($order = Criteria::ASC) Order by the encriptacion column
 * @method ConfiguracionQuery orderByPuerto($order = Criteria::ASC) Order by the puerto column
 * @method ConfiguracionQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method ConfiguracionQuery orderByClave($order = Criteria::ASC) Order by the clave column
 *
 * @method ConfiguracionQuery groupById() Group by the id column
 * @method ConfiguracionQuery groupByHost() Group by the host column
 * @method ConfiguracionQuery groupByEncriptacion() Group by the encriptacion column
 * @method ConfiguracionQuery groupByPuerto() Group by the puerto column
 * @method ConfiguracionQuery groupByUsuario() Group by the usuario column
 * @method ConfiguracionQuery groupByClave() Group by the clave column
 *
 * @method ConfiguracionQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ConfiguracionQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ConfiguracionQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method Configuracion findOne(PropelPDO $con = null) Return the first Configuracion matching the query
 * @method Configuracion findOneOrCreate(PropelPDO $con = null) Return the first Configuracion matching the query, or a new Configuracion object populated from the query conditions when no match is found
 *
 * @method Configuracion findOneById(int $id) Return the first Configuracion filtered by the id column
 * @method Configuracion findOneByHost(string $host) Return the first Configuracion filtered by the host column
 * @method Configuracion findOneByEncriptacion(string $encriptacion) Return the first Configuracion filtered by the encriptacion column
 * @method Configuracion findOneByPuerto(string $puerto) Return the first Configuracion filtered by the puerto column
 * @method Configuracion findOneByUsuario(string $usuario) Return the first Configuracion filtered by the usuario column
 * @method Configuracion findOneByClave(string $clave) Return the first Configuracion filtered by the clave column
 *
 * @method array findById(int $id) Return Configuracion objects filtered by the id column
 * @method array findByHost(string $host) Return Configuracion objects filtered by the host column
 * @method array findByEncriptacion(string $encriptacion) Return Configuracion objects filtered by the encriptacion column
 * @method array findByPuerto(string $puerto) Return Configuracion objects filtered by the puerto column
 * @method array findByUsuario(string $usuario) Return Configuracion objects filtered by the usuario column
 * @method array findByClave(string $clave) Return Configuracion objects filtered by the clave column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseConfiguracionQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseConfiguracionQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Configuracion', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ConfiguracionQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     ConfiguracionQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ConfiguracionQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ConfiguracionQuery) {
            return $criteria;
        }
        $query = new ConfiguracionQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Configuracion|Configuracion[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ConfiguracionPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ConfiguracionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Configuracion A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `HOST`, `ENCRIPTACION`, `PUERTO`, `USUARIO`, `CLAVE` FROM `configuracion` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Configuracion();
            $obj->hydrate($row);
            ConfiguracionPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Configuracion|Configuracion[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Configuracion[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ConfiguracionPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ConfiguracionPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(ConfiguracionPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the host column
     *
     * Example usage:
     * <code>
     * $query->filterByHost('fooValue');   // WHERE host = 'fooValue'
     * $query->filterByHost('%fooValue%'); // WHERE host LIKE '%fooValue%'
     * </code>
     *
     * @param     string $host The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByHost($host = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($host)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $host)) {
                $host = str_replace('*', '%', $host);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ConfiguracionPeer::HOST, $host, $comparison);
    }

    /**
     * Filter the query on the encriptacion column
     *
     * Example usage:
     * <code>
     * $query->filterByEncriptacion('fooValue');   // WHERE encriptacion = 'fooValue'
     * $query->filterByEncriptacion('%fooValue%'); // WHERE encriptacion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $encriptacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByEncriptacion($encriptacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($encriptacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $encriptacion)) {
                $encriptacion = str_replace('*', '%', $encriptacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ConfiguracionPeer::ENCRIPTACION, $encriptacion, $comparison);
    }

    /**
     * Filter the query on the puerto column
     *
     * Example usage:
     * <code>
     * $query->filterByPuerto('fooValue');   // WHERE puerto = 'fooValue'
     * $query->filterByPuerto('%fooValue%'); // WHERE puerto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $puerto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByPuerto($puerto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($puerto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $puerto)) {
                $puerto = str_replace('*', '%', $puerto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ConfiguracionPeer::PUERTO, $puerto, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ConfiguracionPeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the clave column
     *
     * Example usage:
     * <code>
     * $query->filterByClave('fooValue');   // WHERE clave = 'fooValue'
     * $query->filterByClave('%fooValue%'); // WHERE clave LIKE '%fooValue%'
     * </code>
     *
     * @param     string $clave The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function filterByClave($clave = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($clave)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $clave)) {
                $clave = str_replace('*', '%', $clave);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ConfiguracionPeer::CLAVE, $clave, $comparison);
    }

    /**
     * Exclude object from result
     *
     * @param   Configuracion $configuracion Object to remove from the list of results
     *
     * @return ConfiguracionQuery The current query, for fluid interface
     */
    public function prune($configuracion = null)
    {
        if ($configuracion) {
            $this->addUsingAlias(ConfiguracionPeer::ID, $configuracion->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
